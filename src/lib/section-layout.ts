/**
 * Clases compartidas para todas las páginas de sección (Podcast, Tutoriales, etc.).
 * Garantiza el mismo espaciado nav → descripción en toda la web.
 */
export const SECTION_PAGE_WRAPPER = 'py-8 md:py-12';

/** Contenedor del contenido de la sección (sin padding top; el page wrapper lo define). */
export const SECTION_CONTAINER = 'w-full max-w-7xl mx-auto px-4 sm:px-6 md:px-8 pb-20';
